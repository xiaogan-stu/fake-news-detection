# Training hyperparameters (paths filled at runtime)
learning_rate: 5.0e-4
batch_size: 4
gradient_accumulation_steps: 8
num_epochs: 50
warmup_ratio: 0.1
max_grad_norm: 1.0
feature_cache_dir: ""
output_dir: ""
early_stopping_patience: 10
