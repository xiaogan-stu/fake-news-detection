# 实验设计与进展报告

> 面向导师的实验进度汇报文档  
> 项目：基于大语言模型与双曲图神经网络的虚假新闻检测  
> 日期：2026年5月

---

## 目录

1. [数据集选择与介绍](#1-数据集选择与介绍)
2. [代码来源](#2-代码来源)
3. [灵感来源与相关工作](#3-灵感来源与相关工作)
4. [实验设计：分阶段进行](#4-实验设计分阶段进行)
5. [实验思路的创新性分析](#5-实验思路的创新性分析)
6. [评判指标与选择理由](#6-评判指标与选择理由)
7. [当前进度与待完成工作](#7-当前进度与待完成工作)

---

## 1. 数据集选择与介绍

### 1.1 数据集来源

实验采用 **TAGFN（Text-Attributed Graph Fake News）** 数据集，来源于 HuggingFace 上的公开仓库 `kayzliu/TAGFN`。该数据集是专门为**基于传播树的虚假新闻检测**任务构建的，包含两个子集：

| 数据集 | 传播树数量 | 节点总数 | 边总数 | 标签分布 |
|--------|-----------|---------|--------|----------|
| **Politifact** | 314 | 41,054 | 40,740 | 真/假（二分类） |
| **Gossipcop** | 5,464 | 314,262 | 308,798 | 真/假（二分类） |

### 1.2 数据结构

每个子集包含以下文件：

| 文件 | 内容 | 维度 |
|------|------|------|
| `A.txt` | 全局边列表，格式 `src, dst` | `[num_edges, 2]` |
| `graph_labels.npy` | 每棵传播树的真假标签（0=真, 1=假） | `[num_graphs]` |
| `node_graph_id.npy` | 每个节点所属的传播树 ID | `[num_nodes]` |
| `node_time.npy` | 每个节点的时间戳（根节点时间最小） | `[num_nodes]` |
| `train_idx.npy` / `val_idx.npy` / `test_idx.npy` | 传播树级别的训练/验证/测试划分索引 | — |
| `raw_text/part-00000.parquet` | 每个节点的原始文本内容 | `[num_nodes]` |

### 1.3 选择理由

1. **传播树结构**：与普通图分类数据集不同，TAGFN 保留了新闻在社交媒体上的**完整传播路径**（谁转发了谁），这为利用图结构信息进行检测提供了基础。
2. **文本-图双重属性**：每个节点同时包含文本内容（推文/转发内容）和图结构位置（在传播树中的层级），适合验证"LLM 文本理解 + GNN 结构建模"的联合方法。
3. **双数据集验证**：Politifact（政治事实核查）和 Gossipcop（娱乐八卦核查）覆盖不同领域的虚假新闻，可用于**跨数据集泛化性验证**。
4. **公开可用**：数据集在 HuggingFace 上公开，保证了实验的可复现性。

### 1.4 数据预处理流程

```
原始 TAGFN 数据
    │
    ├──→ 03_extract_features.py  →  Llama-3-8B (4-bit) 提取 4096 维特征 → node_features.pt
    │
    └──→ 12_extract_bert_features.py  →  BERT-base 提取 768 维特征 → bert_features_{dataset}.pt
```

特征提取后，训练阶段直接加载 `.pt` 文件，无需重复运行 LLM，将显存需求从 ~20GB 降至 < 4GB。

---

## 2. 代码来源

### 2.1 项目代码结构

本项目代码为**自主开发**，但参考了以下开源仓库的设计思路：

| 来源 | 用途 | 引用方式 |
|------|------|----------|
| **[guoxinyu0617/MHR](https://github.com/guoxinyu0617/MHR)** | 洛伦兹流形上的数学操作（指数映射、对数映射、投影等） | 参考其 `manifolds/lorentz.py` 的数学实现，本项目做了简化适配 |
| **[kayzliu/TAGFN](https://huggingface.co/kayzliu/TAGFN)** | 数据集下载与格式解析 | 通过 `02_download_dataset.py` 从 HuggingFace 下载原始数据 |
| **PyTorch Geometric (PyG)** | 图神经网络基础组件（GCNConv, GATConv, SAGEConv, DataLoader） | 作为依赖库直接使用 |
| **HuggingFace Transformers** | Llama-3-8B 和 BERT-base 模型加载与推理 | 作为依赖库直接使用 |
| **bitsandbytes** | Llama-3-8B 的 4-bit 量化（NF4），使特征提取可在 24GB 显存上运行 | 作为依赖库直接使用 |
| **geoopt** | RiemannianAdam 优化器，用于双曲空间中的参数优化 | 作为依赖库直接使用 |

### 2.2 自主开发的核心模块

以下模块为完全自主设计和实现：

- `src/models/mhr_llm.py` — 双曲假新闻检测器（HyperbolicFakeNewsDetector），包含 MLP 降维 → 洛伦兹流形投影 → 多层双曲图卷积 → 根节点分类的完整 pipeline
- `src/models/baselines.py` — 5 个基线模型（MLPDetector, GCNDetector, GATDetector, SAGEDetector, BiGCNDetector），统一接口设计
- `src/models/layers/hgnn_layer.py` — 双曲图卷积层（LorentzLinear + LorentzAgg），参考 MHR 的数学原理但独立实现
- `src/data/tagfn_dataset.py` — TAGFN 数据集加载器，支持 Politifact 和 Gossipcop 两个子集
- `scripts/run_experiment.py` — 统一实验运行框架，支持命令行参数控制所有实验变量
- `scripts/run_all_experiments.sh` — 批量实验运行脚本
- `scripts/collect_results.py` — 实验结果收集与论文表格生成

---

## 3. 灵感来源与相关工作

### 3.1 核心灵感来源

本实验的核心思路来源于以下两条研究线的交叉：

#### 线1：图神经网络用于虚假新闻检测

虚假新闻在社交媒体上的传播形成天然的**树形/图结构**，近年来研究者开始利用 GNN 建模传播模式：

- **BiGCN**（Bian et al., AAAI 2020）— "Rumor Detection on Social Media with Bi-Directional Graph Convolutional Networks"：提出双向图卷积网络，分别建模信息自顶向下（根→叶）和自底向上（叶→根）的传播路径。这是谣言检测领域的重要基线方法。
- **GCN**（Kipf & Welling, ICLR 2017）、**GAT**（Veličković et al., ICLR 2018）、**GraphSAGE**（Hamilton et al., NeurIPS 2017）：经典图神经网络架构，本实验将其作为欧氏空间基线。

#### 线2：双曲空间表示学习

传播树具有天然的**层次结构**和**指数增长**特性，而欧氏空间难以高效嵌入树形结构：

- **HGCN**（Chami et al., NeurIPS 2019）— "Hyperbolic Graph Convolutional Neural Networks"：首次将图卷积推广到双曲空间（Poincaré 球模型），证明双曲空间在建模层次结构时优于欧氏空间。
- **MHR 仓库**（guoxinyu0617/MHR）：提供了洛伦兹流形（Lorentz manifold）上的完整数学操作实现，包括指数映射、对数映射、平行传输等。洛伦兹模型相比 Poincaré 模型在数值稳定性上更有优势。

#### 线3：大语言模型用于文本特征增强

传统方法使用 Word2Vec 或 BERT 提取文本特征，但预训练语言模型的语义理解能力有限：

- 近两年（2024-2025）的研究趋势是将 **LLaMA、GPT 等大语言模型** 引入虚假新闻检测，利用其强大的语义理解能力提取更丰富的文本表示。
- 但端到端训练 LLM + GNN 的显存开销极大（> 40GB），本实验采用**离线特征提取**策略，将 LLM 仅用于特征编码阶段，训练时只加载预提取的特征向量。

### 3.2 本实验的定位

本实验的核心思路是：**将上述三条线结合**——用 Llama-3-8B 提取高质量文本特征，用洛伦兹双曲图神经网络在传播树上进行消息传递，最终对新闻真实性做二分类。这一组合在现有文献中尚不多见。

### 3.3 近两年相关工作的初步调研（需导师协助确认）

> **注意**：以下为基于公开信息的初步调研，具体论文标题和发表信息需要进一步在 Google Scholar / DBLP 上核实。标注 `[待核实]` 的条目请导师帮忙确认。

- **LLM + Fake News Detection**：2024-2025 年出现了多篇将 LLM 用于虚假新闻检测的工作，包括利用 LLM 作为特征提取器、利用 LLM 进行数据增强、以及利用 LLM 进行零样本检测等方向 `[待核实具体论文]`。
- **Hyperbolic GNN for NLP**：双曲空间在自然语言处理中的应用持续受到关注，特别是在建模层次结构（如知识图谱、句法树）方面 `[待核实具体论文]`。
- **传播树 + 双曲空间**：将新闻传播树建模在双曲空间中的工作相对较少，这是本实验的主要创新点所在。

---

## 4. 实验设计：分阶段进行

### 4.1 实验总体框架

所有实验通过统一的 `scripts/run_experiment.py` 入口运行，通过命令行参数控制三个核心变量：

| 变量 | 可选值 | 含义 |
|------|--------|------|
| `--model_type` | hgnn / gcn / gat / sage / bigcn / mlp / gatv2 | 模型架构 |
| `--feat_type` | llama / bert | 节点特征类型（4096d / 768d） |
| `--dataset` | politifact / gossipcop | 数据集 |

### 4.2 阶段一：Politifact 基线对比（BERT 特征）

**目的**：在统一的 BERT 特征下，比较不同图神经网络架构的性能，建立基线。

| 实验名称 | 模型 | 特征 | 说明 |
|----------|------|------|------|
| `bert_mlp_poli` | MLP | BERT (768d) | 纯文本基线，不使用图结构 |
| `bert_gcn_poli` | GCN | BERT (768d) | 图卷积网络 |
| `bert_gat_poli` | GAT | BERT (768d) | 图注意力网络（4头） |
| `bert_sage_poli` | GraphSAGE | BERT (768d) | 归纳式图网络 |
| `bert_bigcn_poli` | BiGCN | BERT (768d) | 双向图卷积（谣言检测专用） |

**为什么选择这些对比方法：**

- **MLP**：验证图结构是否提供了额外信息（无图 vs 有图）
- **GCN**：最基础的图卷积方法，作为 GNN 基线的下界
- **GAT**：引入注意力机制，验证不同邻居节点的重要性差异是否有帮助
- **GraphSAGE**：归纳式方法，验证采样聚合策略在传播树上的效果
- **BiGCN**：谣言检测领域的代表性方法，建模双向信息流

### 4.3 阶段二：LLM 增强基线（Llama 特征）

**目的**：验证 Llama-3-8B 提取的 4096 维特征相比 BERT 768 维特征是否有显著提升。

| 实验名称 | 模型 | 特征 | 说明 |
|----------|------|------|------|
| `llama_mlp_poli` | MLP | Llama (4096d) | LLM 特征 + 无图结构 |
| `llama_gcn_poli` | GCN | Llama (4096d) | LLM 特征 + 图卷积 |
| `llama_gatv2_poli` | GATv2 | Llama (4096d) | LLM 特征 + 动态注意力（欧氏空间最强基线） |
| `llama_hgnn_poli` | **HGNN (Ours)** | Llama (4096d) | **本文方法**：LLM 特征 + 双曲图卷积 |

**为什么选择 GATv2 而非 GAT：**
GATv2（Brody et al., ICLR 2022）修复了 GAT 的静态注意力问题，使注意力分数可以动态依赖于查询节点，理论上比 GAT 更强。将其作为欧氏空间中的最强基线，与双曲方法进行公平对比。

### 4.4 阶段三：消融实验

**目的**：逐一移除本文方法的关键组件，验证每个组件的贡献。

| 实验名称 | 模型 | 特征 | 消融内容 | 对照的完整模型 |
|----------|------|------|----------|---------------|
| `llama_gatv2_poli` | GATv2 | Llama | **w/o Hyperbolic**（去掉双曲空间，用欧氏 GATv2 替代） | llama_hgnn_poli |
| `ablation_bert_hgnn` | HGNN | BERT | **w/o LLM**（用 BERT 替代 Llama 特征） | llama_hgnn_poli |
| `llama_mlp_poli` | MLP | Llama | **w/o Graph**（去掉图结构，仅用 MLP） | llama_hgnn_poli |
| `ablation_1layer` | HGNN | Llama | **w/o Multi-layer**（仅 1 层双曲卷积） | llama_hgnn_poli (2层) |
| `ablation_3layer` | HGNN | Llama | **3 层**（验证更深网络是否更好） | llama_hgnn_poli (2层) |

**消融实验的严谨性设计：**

1. **控制变量法**：每次只改变一个组件，其余条件完全相同
2. **公平对比**：GATv2 使用与 HGNN 完全相同的 Llama 特征和训练配置
3. **层数探索**：1/2/3 层对比，验证最优深度

### 4.5 阶段四：跨数据集泛化性验证

**目的**：验证方法在不同领域数据上的泛化能力。

| 实验名称 | 模型 | 特征 | 数据集 |
|----------|------|------|--------|
| `bert_gcn_gossip` | GCN | BERT | Gossipcop |
| `bert_gat_gossip` | GAT | BERT | Gossipcop |
| `bert_hgnn_gossip` | **HGNN (Ours)** | BERT | Gossipcop |

> 注：Gossipcop 的 Llama 特征提取需要约 314K 节点 × 4096 维 ≈ 2.4GB 存储，且推理时间较长（约 8-12 小时），因此跨数据集阶段优先使用 BERT 特征进行验证。

### 4.6 实验配置的统一性

所有实验使用完全相同的训练配置，确保公平对比：

| 超参数 | 值 | 说明 |
|--------|-----|------|
| 学习率 | 5e-4 | AdamW / RiemannianAdam |
| 批大小 | 4 | 图级别 batch |
| 梯度累积 | 8 步 | 等效 batch size = 32 |
| 最大 epoch | 50 | 配合早停 |
| 早停耐心值 | 10 | 验证集 F1 不再提升则停止 |
| 学习率调度 | Warmup (10%) + Linear Decay | 稳定训练 |
| 梯度裁剪 | max_norm=1.0 | 防止梯度爆炸 |
| 随机种子 | 42 | 保证可复现 |
| GNN 隐藏维度 | 128 | 所有模型统一 |
| Dropout | 0.1 | 所有模型统一 |

---

## 5. 实验思路的创新性分析

### 5.1 核心创新点

1. **LLM + 双曲 GNN 的联合框架**：
   - 现有虚假新闻检测工作大多使用 Word2Vec/BERT 等轻量级编码器，或端到端微调中小型语言模型
   - 本实验将 Llama-3-8B（4-bit 量化）作为离线特征提取器，在保持 LLM 语义理解能力的同时，将训练显存控制在 4GB 以内
   - 这种"LLM 编码 + GNN 分类"的解耦设计在工程上具有实用性

2. **洛伦兹流形上的传播树建模**：
   - 新闻传播树具有天然的层次结构和指数增长特性
   - 双曲空间（特别是洛伦兹模型）在数学上更适合嵌入树形结构
   - 本实验将双曲图卷积应用于虚假新闻检测这一具体任务，属于应用创新

3. **全面的消融实验设计**：
   - 系统性地验证了三个核心组件（LLM 特征、双曲空间、图结构）各自的贡献
   - 通过控制变量法确保结论的可靠性

### 5.2 与现有工作的区别

| 维度 | 现有典型工作 | 本实验 |
|------|-------------|--------|
| 文本编码器 | Word2Vec / BERT / RoBERTa | **Llama-3-8B (4-bit)** |
| 图神经网络 | 欧氏空间 GCN/GAT | **洛伦兹双曲 GNN** |
| 特征维度 | 300-768 | **4096** |
| 训练方式 | 端到端（显存 > 20GB） | **离线特征 + 轻量训练（< 4GB）** |
| 消融实验 | 通常 2-3 组 | **5 组系统消融** |
| 跨数据集 | 部分工作有 | **Politifact + Gossipcop 双数据集** |

### 5.3 出发论文与理论基础

本实验的学术出发点可以追溯到以下论文（按时间线）：

1. **HGCN** (Chami et al., NeurIPS 2019)：首次证明双曲空间在图表示学习中的优势，为本实验使用双曲空间提供了理论动机。
2. **BiGCN** (Bian et al., AAAI 2020)：将双向图卷积用于谣言检测，为本实验的基线选择和传播树建模提供了参考。
3. **MHR 相关论文** `[待核实]`：guoxinyu0617/MHR 仓库对应的论文，提供了洛伦兹流形上 GNN 的完整数学框架，本实验的 `lorentz.py` 和 `hgnn_layer.py` 参考了其实现。

> **需要导师协助**：请帮忙确认 MHR 仓库对应的正式发表论文标题及会议/期刊，以及近两年（2024-2025）是否有直接结合 LLM 和双曲 GNN 进行虚假新闻检测的已发表工作。

---

## 6. 评判指标与选择理由

### 6.1 使用的指标

| 指标 | 全称 | 计算方式 | 取值范围 |
|------|------|----------|----------|
| **Accuracy** | 准确率 | (TP + TN) / (TP + TN + FP + FN) | [0, 1] |
| **F1-macro** | 宏平均 F1 分数 | 各类别 F1 的算术平均 | [0, 1] |
| **F1-weighted** | 加权 F1 分数 | 按类别样本数加权的 F1 平均 | [0, 1] |
| **Precision-macro** | 宏平均精确率 | 各类别 Precision 的算术平均 | [0, 1] |
| **Recall-macro** | 宏平均召回率 | 各类别 Recall 的算术平均 | [0, 1] |
| **AUC** | ROC 曲线下面积 | 基于预测概率计算 | [0, 1] |

### 6.2 选择理由

1. **Accuracy（准确率）**：
   - 最直观的分类性能指标，便于与非技术背景的读者沟通
   - 在类别相对均衡的数据集上（Politifact 和 Gossipcop 的标签分布接近平衡），Accuracy 是可靠的

2. **F1-macro（宏平均 F1）**：
   - 对每个类别平等对待，不受类别不平衡影响
   - 在虚假新闻检测中，假新闻（正类）和真新闻（负类）的检测同等重要
   - 作为**早停（Early Stopping）的监控指标**，因为它是 Precision 和 Recall 的调和平均，比单独使用 Accuracy 更稳健

3. **F1-weighted（加权 F1）**：
   - 考虑类别样本量差异，反映整体分类质量
   - 与 F1-macro 配合使用，可以判断模型是否偏向多数类

4. **Precision-macro & Recall-macro**：
   - 分别衡量"预测为假新闻中有多少是真的假新闻"和"所有假新闻中有多少被检测出来"
   - 在实际应用中，两者往往需要权衡（高 Precision 意味着少误报，高 Recall 意味着少漏报）

5. **AUC（ROC 曲线下面积）**：
   - 不依赖于具体分类阈值，衡量模型的整体排序能力
   - 在类别不平衡或需要灵活调整阈值时尤为重要
   - 是学术论文中最常用的综合指标之一

### 6.3 主指标选择

**论文中以 F1-macro 和 AUC 作为主要汇报指标**，理由如下：
- F1-macro 是分类任务的标准指标，兼顾 Precision 和 Recall
- AUC 是排序质量的综合度量，不受阈值选择影响
- 两者结合可以全面评估模型的分类和排序能力

---

## 7. 当前进度与待完成工作

### 7.1 已完成

| 工作项 | 状态 | 说明 |
|--------|------|------|
| 项目框架搭建 | ✅ 完成 | 完整的目录结构、模型定义、数据加载、实验框架 |
| 双曲模型实现 | ✅ 完成 | HyperbolicFakeNewsDetector，含洛伦兹流形操作 |
| 基线模型实现 | ✅ 完成 | MLP / GCN / GAT / GraphSAGE / BiGCN / GATv2，共 6 个基线 |
| 数据集加载器 | ✅ 完成 | 支持 Politifact 和 Gossipcop 两个子集 |
| Llama 特征提取脚本 | ✅ 完成 | 4-bit 量化，支持断点续传 |
| BERT 特征提取脚本 | ✅ 完成 | 支持断点续传 |
| 统一实验框架 | ✅ 完成 | run_experiment.py，命令行参数控制所有变量 |
| 批量实验脚本 | ✅ 完成 | run_all_experiments.sh，按阶段顺序运行 |
| 结果收集脚本 | ✅ 完成 | collect_results.py，生成 Markdown + LaTeX 表格 |
| 代码调试与修复 | ✅ 完成 | 修复了导入错误、维度不匹配、路径不一致、参数名错误等问题 |

### 7.2 待完成（需在服务器上运行）

| 工作项 | 优先级 | 预计耗时 | 说明 |
|--------|--------|----------|------|
| 下载 TAGFN 数据集 | 🔴 高 | ~30 min | 运行 `02_download_dataset.py` |
| 下载 Llama-3-8B 模型 | 🔴 高 | ~30 min | 从 HuggingFace 下载到 `/mnt/workspace/model/` |
| 提取 Llama 特征 | 🔴 高 | ~4-6 h | 运行 `03_extract_features.py`，41K 节点 |
| 提取 BERT 特征 | 🔴 高 | ~1-2 h | 运行 `12_extract_bert_features.py` |
| 运行全部实验 | 🔴 高 | ~12-24 h | 运行 `run_all_experiments.sh`，约 16 个实验 |
| 生成论文表格 | 🟡 中 | ~5 min | 运行 `collect_results.py` |
| 结果分析与论文撰写 | 🟡 中 | — | 根据实验结果撰写论文 |

### 7.3 需要导师协助确认的事项

1. **MHR 仓库对应的正式论文**：guoxinyu0617/MHR 的论文标题、作者、发表会议/期刊
2. **近两年相关工作的完整性**：2024-2025 年是否有直接结合 LLM 和双曲 GNN 进行虚假新闻检测的已发表工作
3. **实验设计的合理性**：基线选择、消融设计、指标选择是否合理
4. **论文投稿目标**：建议投哪个会议/期刊（如 ACL、EMNLP、WWW、CIKM 等）

---

## 附录：实验运行命令速查

```bash
# 1. 下载数据集
python scripts/02_download_dataset.py --dataset politifact --output /mnt/workspace/data/politifact
python scripts/02_download_dataset.py --dataset gossipcop --output /mnt/workspace/data/gossipcop

# 2. 提取特征
python scripts/03_extract_features.py --dataset politifact
python scripts/12_extract_bert_features.py --dataset politifact

# 3. 单个实验示例
python scripts/run_experiment.py \
    --model_type hgnn --feat_type llama --dataset politifact \
    --exp_name llama_hgnn_poli

# 4. 批量运行全部实验
bash scripts/run_all_experiments.sh

# 5. 收集结果生成表格
python scripts/collect_results.py
```